# Make Webhook (REQUIRED)
# Get this from: make.com → create scenario → add webhook trigger
# All form submissions are sent here as JSON
MAKE_WEBHOOK_URL=https://hook.eu2.make.com/your_webhook_id

# AI Analysis (OPTIONAL - set to "true" to enable)
# When enabled, generates AI-powered battlecards for admin review
# When disabled, only raw form data is sent to Make webhook
ENABLE_AI_ANALYSIS=false

# OpenAI API Key (only needed if ENABLE_AI_ANALYSIS=true)
OPENAI_API_KEY=sk-your-openai-key-here
